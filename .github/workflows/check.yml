# Generated by OmniShip from workflow.py. Do not edit directly.
name: Check
'on':
  pull_request: {}
  push:
    branches:
    - main
  workflow_call: {}
permissions:
  contents: read
jobs:
  prepare:
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
    - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
    - run: uv sync --all-groups --locked
    - run: uv run omniship generate --check
  check-ruff:
    needs: prepare
    runs-on: ubuntu-slim
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
    - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
    - run: uv sync --all-groups --locked
    - run: uv run omniship run-node --stage check --node ruff --config omniship.yaml
  check-pytest:
    needs: prepare
    runs-on: ${{ matrix.runner }}
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
    - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
    - run: uv sync --all-groups --locked
    - run: uv run omniship run-node --stage check --node pytest --config omniship.yaml
    strategy:
      fail-fast: false
      matrix:
        runner:
        - ubuntu-24.04
        - macos-15
        - windows-2025
  check-complete:
    needs:
    - check-ruff
    - check-pytest
    runs-on: ubuntu-24.04
    steps:
    - run: echo 'check stage complete'
