# Generated by OmniShip from workflow.py. Do not edit directly.
name: Build
'on':
  workflow_call: {}
permissions:
  contents: read
jobs:
  check:
    uses: ./.github/workflows/check.yml
  build-wheel:
    needs: check
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
    - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
    - run: uv sync --all-groups --locked
    - run: uv run omniship run-node --stage build --node wheel --config omniship.yaml --export-artifacts .omniship/handoff/build-wheel
    - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: omniship-build-build-wheel
        path: .omniship/handoff/build-wheel
        include-hidden-files: true
  build-complete:
    needs: build-wheel
    runs-on: ubuntu-24.04
    steps:
    - run: echo 'build stage complete'
